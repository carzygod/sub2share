# 管理员共享资源横向钻取

实现日期：2026-06-12

## 背景

共享资源是 Sub2API/OpenAI 上游、供给方、用量结算和提现之间的核心连接点。此前管理后台已经有供给方列表、共享资源列表、资源详情、最近用量、最近结算和凭据应用日志，但很多证据仍是只读文本。管理员处理 `resources`、`resourceCredentials` 或 `sub2` 巡检问题时，需要复制资源 ID、Sub2 账号、供给方邮箱或请求 ID 再切换页面。

## 已实现范围

- `供给方管理` 行新增用户、资源、提现直达操作。
- `供给方管理` 的最近资源行新增打开资源操作。
- `共享资源池` 行新增供给方、反代状态、用量、结算直达操作。
- `资源详情` 顶部新增供给方、反代状态、用量、结算和提现直达操作。
- `资源详情` 的供给方区块新增打开用户和提现操作。
- `资源详情` 的最近用量新增操作列，可打开用量、用户、租赁和反代请求。
- `资源详情` 的最近结算新增操作列，可打开结算和对应用量。
- `最近凭据应用` 新增操作列，可打开 Sub2 反代状态和关联反代请求。

## 管理价值

- 管理员可以从单个共享资源串联供给方、Sub2/OpenAI 账号、用量、反代请求、结算和提现。
- 当前线上 `resources`、`resourceCredentials`、`sub2` 巡检问题对应的人工修复入口更短。
- 复用既有管理列表、详情加载和权限边界，不新增绕过后台入口的隐藏操作。

## 验证

- `pnpm.cmd --filter @zyz/admin run typecheck`
- `pnpm.cmd --filter @zyz/admin test`
- `pnpm.cmd build`
