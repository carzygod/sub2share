{
  "name": "@zyz/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"shared tests pending\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}

